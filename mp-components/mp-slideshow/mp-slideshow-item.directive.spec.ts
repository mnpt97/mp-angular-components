import { MpSlideshowItemDirective } from './mp-slideshow-item.directive';

describe('MpSlideshowItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MpSlideshowItemDirective();
    expect(directive).toBeTruthy();
  });
});
