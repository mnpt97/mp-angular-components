import { MpPinchZoomDirective } from './mp-pinch-zoom.directive';

describe('MpPinchZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new MpPinchZoomDirective();
    expect(directive).toBeTruthy();
  });
});
